* Chạy thành công server.

* Build model dựa trên database.

	- Xây dựng các class.

* Xây dựng package utils, có : ConnectionUtil, chứa getConnection(), chứa DriverManager, jdbc.

* Xây dựng package repository, có :

	Interface CRUDRepository, Interface CRUDObject.

	Implement các interface CRUDObject.

* Xây dựng package service, có : 

	Interface CRUDServcice, Interface CRUDObject.

	Implement các interface CRUDObject. (Được dùng để gọi repository).